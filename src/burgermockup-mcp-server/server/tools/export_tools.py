"""export_listing — STUB. Real implementation is a stretch bonus, gated until
the core flows are green. When built it must: validate every requested
variant_id against the caller-supplied variant set, and name zip members from
server-side constants (mockup_01.png), never from caller strings."""

from __future__ import annotations


def export_listing(variant_ids: list[str]) -> dict:
    """Export mockups as a listing package (zip + listing.json). Not available
    yet — coming after the core generation flows."""
    return {"status": "not_implemented",
            "message": "export is a roadmap feature in this build"}
