# Pinned: the transport spike (tests/spike_*.py) verified progress-notification
# delivery and the job-abort pattern over streamable HTTP on EXACTLY these
# versions. Bump only after re-running the spike.
fastmcp==3.4.1
mcp==1.27.2
pillow==12.2.0
httpx==0.28.1
opencv-python-headless==4.*
# Lazy-imported by pipeline/scene_gen.py — tests mock it, so a missing install
# only surfaces at the first live lifestyle call (degrades silently to flat).
google-genai==2.8.0
python-dotenv>=1.0
numpy==2.*
pytest==8.*
pytest-asyncio==0.*
